<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_clok"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_clok_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Clok"
        tools:targetApi="34">
        <activity
            android:name="com.kingfu.clok.mainActivity.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Clok.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver
            android:name="com.kingfu.clok.notification.timer.TimerNotificationReceiver"
            android:exported="false" />
    </application>

</manifest>